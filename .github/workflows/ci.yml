name: CI

on:
  push:
    branches: [main, tests]
  pull_request:
    branches: [main, tests]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  flipper-app:
    name: Flipper app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install ufbt
        run: pip install ufbt

      - name: Build FAP
        run: cd flipper-app && ufbt build

      - name: Run protocol C tests
        run: cd flipper-app/tests && make test

      - name: Upload FAP artifact
        uses: actions/upload-artifact@v4
        with:
          name: claude_buddy-fap
          path: ~/.ufbt/build/claude_buddy.fap
          if-no-files-found: error

  claude-host-bridge:
    name: Claude host bridge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install test dependencies
        run: pip install pytest pytest-asyncio pyserial pyserial-asyncio bleak

      - name: Run tests
        working-directory: plugin/host-bridge
        env:
          PYTHONPATH: .
        run: pytest -q

  codex-host-bridge:
    name: Codex host bridge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install test dependencies
        run: pip install pytest pytest-asyncio pyserial pyserial-asyncio bleak

      - name: Run tests
        working-directory: flipper-codex-buddy/host-bridge
        env:
          PYTHONPATH: .
        run: pytest -q

  codex-plugin:
    name: Codex plugin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate plugin manifest and hooks
        run: |
          python3 <<'PY'
          import json
          from pathlib import Path

          root = Path("flipper-codex-buddy")
          manifest = json.loads((root / ".codex-plugin" / "plugin.json").read_text(encoding="utf-8"))
          hooks = json.loads((root / "hooks" / "hooks.json").read_text(encoding="utf-8"))
          marketplace = json.loads(Path(".agents/plugins/marketplace.json").read_text(encoding="utf-8"))

          assert manifest["name"] == "flipper-codex-buddy"
          for key in ("name", "version", "description", "interface"):
              assert key in manifest, key

          assert "hooks" in hooks
          assert any(
              hook_entry.get("hooks")
              for entries in hooks["hooks"].values()
              for hook_entry in entries
          )

          plugin_names = [entry["name"] for entry in marketplace["plugins"]]
          assert "flipper-codex-buddy" in plugin_names
          print("Codex plugin validation passed")
          PY

      - name: Check install and bridge scripts are executable
        run: |
          test -x flipper-codex-buddy/scripts/install-codex-plugin.sh
          test -x flipper-codex-buddy/bin/flipper-bridge

  cursor-host-bridge:
    name: Cursor host bridge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install test dependencies
        run: pip install pytest pytest-asyncio pyserial pyserial-asyncio bleak

      - name: Run tests
        working-directory: flipper-cursor-buddy/host-bridge
        env:
          PYTHONPATH: .
        run: pytest -q

  cursor-plugin:
    name: Cursor plugin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate hooks manifest
        run: |
          python3 <<'PY'
          import json
          from pathlib import Path

          root = Path("flipper-cursor-buddy")
          hooks = json.loads((root / ".cursor" / "hooks.json").read_text(encoding="utf-8"))

          assert hooks.get("version") == 1
          assert "hooks" in hooks
          required = {
              "sessionStart", "sessionEnd", "beforeSubmitPrompt", "preToolUse",
              "postToolUse", "preCompact", "stop",
          }
          missing = required - set(hooks["hooks"])
          assert not missing, f"missing hooks: {missing}"
          print("Cursor plugin validation passed")
          PY

      - name: Check install and bridge scripts are executable
        run: |
          chmod +x flipper-cursor-buddy/scripts/install-cursor-hooks.sh
          chmod +x flipper-cursor-buddy/bin/flipper-bridge
          test -x flipper-cursor-buddy/scripts/install-cursor-hooks.sh
          test -x flipper-cursor-buddy/bin/flipper-bridge
